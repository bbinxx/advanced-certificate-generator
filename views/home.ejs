<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800">

<%- include('layout') %>

<div class="min-h-screen bg-gray-50">
  <nav class="bg-white shadow-md">
    <div class="container mx-auto px-8 py-6 flex justify-between items-center">
      <h1 class="text-3xl font-bold text-blue-600">Certificate Portal</h1>
      <div class="flex items-center space-x-6">
        <span id="userEmail" class="text-gray-700 font-medium">Welcome, <%= user.email %></span>
        <form action="/logout" method="POST">
          <button type="submit" class="bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-6 rounded-lg shadow-md">Log Out</button>
        </form>
      </div>
    </div>
  </nav>
  <main class="container mx-auto px-8 py-12">
    <h2 class="text-2xl font-bold text-gray-800 mb-8">Dashboard</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <a href="/design" class="bg-blue-100 hover:bg-blue-200 text-blue-800 font-semibold py-6 px-8 rounded-lg shadow-md flex items-center justify-center">
        Design Certificates
      </a>
      <a href="/bulk-mode" class="bg-green-100 hover:bg-green-200 text-green-800 font-semibold py-6 px-8 rounded-lg shadow-md flex items-center justify-center">
        Bulk Certificate Generation
      </a>
      <a href="/templates" class="bg-yellow-100 hover:bg-yellow-200 text-yellow-800 font-semibold py-6 px-8 rounded-lg shadow-md flex items-center justify-center">
        Manage Templates
      </a>
    </div>
  </main>
</div>

</body>
</html>
