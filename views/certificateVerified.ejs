<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Certificate Verified</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex justify-center items-center h-screen bg-gray-50">
  <%- include('layout') %>
  <% title = 'Certificate Verified' %>

  <div class="flex justify-center items-center min-h-screen bg-gray-50 py-12 px-4">
    <div class="bg-white p-8 rounded-lg shadow-lg max-w-lg w-full text-center">
        <div class="mb-6">
            <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
                <i class="fas fa-check-circle text-4xl text-green-600"></i>
            </div>
            <h1 class="text-3xl font-bold text-green-600">Certificate Verified</h1>
        </div>
        
        <div class="space-y-4">
            <div class="p-4 bg-gray-50 rounded-lg">
                <p class="text-gray-700"><span class="font-semibold">Name:</span> <%= certificateData.name %></p>
            </div>
            <div class="p-4 bg-gray-50 rounded-lg">
                <p class="text-gray-700"><span class="font-semibold">Certificate ID:</span> <%= certificateData.certificateId %></p>
            </div>
            <div class="p-4 bg-gray-50 rounded-lg">
                <p class="text-gray-700"><span class="font-semibold">Date:</span> <%= certificateData.date %></p>
            </div>
            <div class="p-4 bg-gray-50 rounded-lg">
                <p class="text-gray-700"><span class="font-semibold">Issuer:</span> <%= certificateData.issuer %></p>
            </div>
        </div>

        <div class="mt-8">
            <a href="/" class="inline-flex items-center justify-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 transition-colors duration-200">
                <i class="fas fa-home mr-2"></i> Return Home
            </a>
        </div>
    </div>
</div>
</body>
</html>
